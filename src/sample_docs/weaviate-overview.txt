Weaviate is an open-source, AI-native vector database. It stores both objects and vectors, enabling hybrid search that combines vector similarity with structured filtering. Weaviate supports GraphQL and REST APIs, scales to billions of objects on Kubernetes, and is commonly used for RAG (Retrieval-Augmented Generation), semantic search, and AI application backends. It can run in-cluster and connect via Kubernetes DNS.
