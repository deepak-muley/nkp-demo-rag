RAG (Retrieval-Augmented Generation) augments large language models with external knowledge. Documents are chunked and embedded into a vector database. When a user asks a question, the system retrieves relevant chunks, injects them into the prompt, and the LLM generates an answer grounded in the retrieved content. This reduces hallucinations and keeps knowledge up to date without retraining the model.
