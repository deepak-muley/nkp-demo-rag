apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-rag.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "demo-rag.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "demo-rag.name" . }}
